import type { Metadata } from "next";
import { Inter as FontSans } from "next/font/google";
import "./globals.css";
import { cn } from "@/lib/utils";

const fontSans = FontSans({
  subsets: ["latin"],
  variable: "--font-sans",
});

export const metadata: Metadata = {
  title: "Modern Web Application for Showcasing Company Profiles",
  description:
    "Discover Company Profile, a sleek and responsive web application built with Next.js, React, and Tailwind CSS. Perfect for showcasing your company's profile with style and interactivity. Features include responsive design, interactive components, smooth animations, and effortless API integrations.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={cn(
          "min-h-screen bg-background font-sans antialiased",
          fontSans.variable,
        )}
      >
        {children}
      </body>
    </html>
  );
}
